local folder = "RinnsHubSettings"

    if not isfolder(folder) then
        makefolder(folder)
    end

local folder1 = "RinnsHubTheme"

    if not isfolder(folder1) then
        makefolder(folder1)
    end
